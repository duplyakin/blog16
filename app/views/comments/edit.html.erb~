<h2>Update a comment:</h2>
<p style="color: green;"><%= flash[:success] %></p>
<p style="color: red;"><%= flash[:error] %></p>
  <%= form_for(@comment) do |f| %>
   
    <div class="field">
      <%= f.label :body %><br />
      <%= f.text_area :body %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<br />

<%= link_to 'Edit Post', edit_post_path(@comment.post) %> |
<%= link_to 'Back to Posts', posts_path %> |

